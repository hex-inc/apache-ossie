# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.4",
    "syrupy>=6.0.0",
    "inline-snapshot>=0.35.4",
    "rumdl>=0.2.60",
    "poethepoet>=0.48.0",
    "ty>=0.0.77",
]

[project]
name = "ossie-hex"
version = "0.2.0.dev0"
description = "Bidirectional converter between Apache Ossie semantic models and Hex semantic projects"
authors = [{ name = "Apache Software Foundation", email = "dev@ossie.apache.org" }]
requires-python = ">=3.11"
readme = "README.md"
license = "Apache-2.0"
keywords = [
    "Apache Ossie",
    "Ossie",
    "Hex",
]
dependencies = [
    "apache-ossie>=0.2.0.dev0",
    "hex-sl-utils>=0.2.0.dev0",
    "pydantic>=2.13.4",
    "pydantic-core>=2.46.4",
    "PyYAML>=6.0",
    "typing-extensions>=4.16.0",
]

[project.scripts]
ossie-hex = "ossie_hex.cli:main"

[project.urls]
homepage = "https://ossie.apache.org/"
repository = "https://github.com/apache/ossie/"

[tool.hatch.build.targets.wheel]
packages = ["src/ossie_hex"]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]

[tool.inline-snapshot]
format-command = "ruff format --stdin-filename={filename} -"

[tool.ruff]
target-version = "py311"

[tool.rumdl]
flavor = "gfm"

[tool.rumdl.MD013]
reflow = true

[tool.ty.environment]
python-version = "3.11"

[tool.ty.src]
include = ["src", "tests"]

[tool.poe.tasks]
check = [
    { cmd = "ruff check src tests" },
    { cmd = "ruff format --check src tests" },
    { cmd = "rumdl check ." },
    { cmd = "rumdl fmt --check ." },
    { cmd = "ty check" },
]
format = [
    { cmd = "ruff check --fix src tests" },
    { cmd = "ty check --fix" },
    { cmd = "ruff format src tests" },
    { cmd = "rumdl fmt ." },
]
test = "pytest"

# apache-ossie is not yet published to PyPI; resolve it from the in-repo
# package for now. Remove this block once apache-ossie is published to PyPI.
[tool.uv.sources]
apache-ossie = { path = "../../python", editable = true }
